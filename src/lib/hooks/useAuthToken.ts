'use client';

/**
 * Legacy no-op hook.
 *
 * Previously synced tokens into browser storage; tokens are now managed
 * exclusively by NextAuth cookies/JWT.
 */
export function useAuthToken() {
  // Intentionally empty.
}




