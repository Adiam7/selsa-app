feat: production readiness - Dockerfile, Vercel, error pages, env hardening

- Upgrade Dockerfile to Node 20, standalone output, build-time ARGs for
  NEXT_PUBLIC_* env vars, smaller final image
- Add vercel.json with build/install commands, security headers, API rewrites
- Add next.config.ts: standalone output (skip on Vercel), dynamic CSP with
  BACKEND_URL, Cloudinary remote images, conditional media proxy
- Add error.tsx, global-error.tsx, not-found.tsx error boundary pages
- Harden BACKEND_URL in all API route handlers (cart/guest, cart/my, cart/route,
  guest-create) with fallback chain
- Fix mergeGuestCart: remove duplicate /api prefix
- Fix products.ts: graceful null return instead of throw when API_BASE_URL missing
- Fix api.ts and public.ts: add BACKEND_URL fallback chain
- Require NEXTAUTH_SECRET (remove insecure default in authOptions.ts)
